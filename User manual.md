# User Manual — Subtitle Localizer v1.1.0

## 1. 이 프로그램은 무엇을 하나요?
영문 SRT 파일을 올리고 원하는 언어를 선택하면, 원본 자막 번호와 타임코드를 그대로 유지한 각 언어별 SRT를 생성합니다.

기본 흐름:

```text
영문 SRT 업로드
→ 대상 언어 선택
→ 번역 스타일 / Glossary 설정
→ AI 현지화
→ 원문과 번역 비교
→ SRT 또는 ZIP 다운로드
→ YouTube Studio에 업로드
```

## 2. 처음 배포할 때 필요한 환경변수
Vercel 프로젝트의 Environment Variables에 다음을 넣습니다.

```env
OPENAI_API_KEY=...
OPENAI_TRANSLATION_MODEL=gpt-5.6-luna
SUBTITLE_APP_ACCESS_KEY=충분히-긴-랜덤-문자열
```

`SUBTITLE_APP_ACCESS_KEY`는 공개된 웹 주소를 다른 사람이 이용해 운영자의 OpenAI API 비용을 쓰는 것을 막는 간단한 보호 키입니다.

## 3. 기본 사용법
1. 첫 화면에 영문 `.srt` 파일을 드롭하거나 클릭해서 선택합니다.
2. 자막 cue 수, 영상 길이, 처리 청크 수를 확인합니다.
3. 번역할 언어를 선택합니다.
   - `추천 8개`: 주요 대상 언어를 빠르게 선택
   - `전체 선택`: 현재 지원 언어 전체
   - `선택 해제`: 모두 해제
4. 번역 스타일을 선택합니다.
5. 필요한 경우 Glossary를 입력합니다.
6. Vercel에서 운영 중이면 `배포 보호 키`에 `SUBTITLE_APP_ACCESS_KEY` 값을 입력합니다.
7. `N개 언어 번역 시작`을 누릅니다.
8. 언어별 진행률을 확인합니다.
9. 완료된 언어는 `SRT` 버튼으로 바로 다운로드할 수 있습니다.
10. 오른쪽 미리보기에서 원문과 번역을 비교합니다.
11. 여러 언어가 완료되면 ZIP으로 한 번에 다운로드할 수 있습니다.

## 4. 번역 스타일
### 자연스러운 YouTube
직역보다 자연스러운 현지화와 읽기 편한 표현을 우선합니다. 일반적인 YouTube 영상의 기본 추천값입니다.

### 원문 충실
원문의 의미와 톤을 최대한 보존합니다. 인터뷰, 인용, 발언 정확성이 중요한 영상에 적합합니다.

### 짧고 읽기 쉽게
화면에 표시되는 시간이 짧은 영상에서 읽기 쉬운 표현을 우선합니다.

### 교육 / 강의
전문용어와 개념 일관성을 더 중요하게 봅니다.

### 비즈니스
정중하고 전문적인 표현을 사용합니다.

## 5. Glossary 사용법
한 줄에 하나씩 규칙을 적습니다.

```text
OpenAI = OpenAI
ChatGPT = ChatGPT
prompt = 프롬프트
token = 토큰
```

브랜드명과 제품명은 특히 Glossary에 넣는 것이 좋습니다.

## 6. 실패했을 때
v1.1.0에서는 한 언어의 번역이 중간에 실패해도 앞에서 이미 완료된 청크를 브라우저 메모리에 보존합니다.

`이어 재시도`를 누르면 성공한 부분을 다시 번역하지 않고 남은 부분부터 진행합니다. 이 기능은 불필요한 API 비용을 줄이기 위한 것입니다.

단, 다음을 변경하면 이전 부분 결과는 의도적으로 폐기됩니다.
- 번역 스타일
- Glossary
- 대상 언어 구성
- 원본 SRT

서로 다른 설정으로 만든 자막이 한 파일에 섞이는 것을 방지하기 위한 동작입니다.

## 7. 오른쪽 품질 미리보기
번역이 완료되면 각 cue에서 다음을 동시에 확인할 수 있습니다.
- 원본 영어
- 선택한 대상 언어 번역
- 원본 start/end 타임코드
- cue 번호
- 구조 검증 통과 여부

`구조 검증 통과`는 cue 번호와 start/end가 원본과 동일하다는 뜻입니다.

## 8. 결과 파일명
원본이 `video.srt`이고 한국어/일본어/스페인어를 선택했다면 예시는 다음과 같습니다.

```text
video.ko.srt
video.ja.srt
video.es.srt
video-subtitles.zip
```

SRT는 UTF-8 호환성을 위해 BOM을 붙여 다운로드합니다.

## 9. 입력 제한
- `.srt` 파일
- 최대 5MB
- 최대 20,000 cue
- cue 하나의 텍스트 최대 2,000자
- Glossary 최대 8,000자

UTF-8 SRT 사용을 권장합니다.

## 10. YouTube에 올리는 방법
v1.1.0은 SRT 파일 생성까지 자동화합니다.

YouTube Studio에서 해당 영상의 자막 메뉴를 열고, 대상 언어를 추가한 뒤 생성된 `.srt` 파일을 업로드합니다.

YouTube 계정 연결과 자동 업로드는 v1.2.0 예정 기능입니다.

## 11. Vercel 배포 후 첫 테스트
처음부터 17개 언어를 돌리지 말고 다음 순서가 안전합니다.
1. 샘플 SRT 업로드
2. 한국어 하나만 선택
3. 번역 시작
4. 결과 비교
5. 한국어 SRT 다운로드
6. SRT 파일을 텍스트 편집기로 열어 번호와 시간 확인
7. 정상일 때 2~3개 언어 테스트
8. 이후 전체 작업 진행

## 12. 오류 메시지별 확인
### `OPENAI_API_KEY가 설정되지 않았습니다.`
Vercel 환경변수에 OpenAI API 키가 없습니다.

### `서버에 SUBTITLE_APP_ACCESS_KEY가 설정되지 않았습니다.`
프로덕션 보호 키가 설정되지 않았습니다.

### `배포 보호 키가 올바르지 않습니다.`
화면에 입력한 보호 키와 Vercel 환경변수 값이 다릅니다.

### `번역할 자막 데이터가 올바르지 않거나 너무 큽니다.`
특정 cue가 너무 길거나 API 요청 제한을 넘어간 입력입니다.

### `번역 응답의 자막 id가 원본과 일치하지 않습니다.`
AI 응답 구조 검증이 실패한 경우입니다. 해당 언어의 `이어 재시도`를 사용합니다.
